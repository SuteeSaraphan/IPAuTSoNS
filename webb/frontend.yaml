apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: frontend
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: frontend
    spec:
      containers:
      - image: chinglew27/ipautsons-frontend:v1.6
        name: ipautsons
        resources: {}
        volumeMounts:
              - name: nfs-share
                mountPath: /ipautsons
        command: ["yarn", "run", "serve"]
      volumes:
      - name: nfs-share
        persistentVolumeClaim: 
          claimName: example
status: {}
