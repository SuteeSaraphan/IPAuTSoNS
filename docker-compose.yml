version: '3'
services:
  frontend:
    # will build ./ipuatsons/Dockerfile
    build: ./ipuatsons
    ports:
     - "80:80"
    volumes:
     - .:/code
  backend:
    # will build ./backend/Dockerfile
    build: ./backend
    ports:
      - "8000:8000"
