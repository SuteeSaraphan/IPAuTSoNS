apiVersion: batch/v1
kind: Job
metadata:
  name: testascii2
spec:
  template:
    spec:
      containers:
      - name: testascii2
        image: suteesaraphan27/ascii
        volumeMounts:
            - name: myvolume
              mountPath: /www
        command: ["python","ASCII.py","1","/www/ea"]
      restartPolicy: Never
      volumes:
      - name: myvolume
        persistentVolumeClaim: 
          claimName: mypvc